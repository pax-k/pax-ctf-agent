# HexStrike server and MCP runtime installed in the isolated container.
# Specialist command-line tools are installed separately in the Dockerfile.
# angr and pwntools are not exposed by the current narrow MCP profile.
flask>=2.3,<4
requests>=2.31,<3
psutil>=5.9,<6
fastmcp>=0.2,<1
beautifulsoup4>=4.12,<5
selenium>=4.15,<5
webdriver-manager>=4,<5
aiohttp>=3.8,<4
mitmproxy>=9,<11
bcrypt==4.0.1
